{
  "scripts": {
    "build": "gulp",
    "build-single": "gulp single",
    "test": "mocha --timeout 10000"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "cypress": "^12.7.0",
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-cli": "^2.3.0",
    "gulp-htmlsplit": "^1.1.1",
    "gulp-inline": "^0.1.3",
    "gulp-uglify": "^3.0.2"
  }
}
